# - name: fetch swarm status of all nodes
#   community.docker.docker_swarm_info:
#   register: swarm_info
#   become: true

# - name: grab first join token and manager to join
#   set_fact:
#     manager_join_token: "{{ hostvars[item]['swarm_info']['swarm_facts']['JoinTokens']['Manager'] }}"
#     worker_join_token: "{{ hostvars[item]['swarm_info']['swarm_facts']['JoinTokens']['Worker'] }}"
#     swarm_join_member: "{{ item }}"
#   loop: "{{ groups['docker_swarm_managers'] }}"
#   run_once: true
#   when: "('swarm_facts' in hostvars[item]['swarm_info']) and (manager_join_token is not defined)"

# - name: check for conflicting join tokens
#   fail:
#     msg: conflicting join tokens found!
#   loop: "{{ groups['docker_swarm_managers'] }}"
#   run_once: true
#   when: "('swarm_facts' in hostvars[item]['swarm_info']) and (manager_join_token is defined) and (manager_join_token!=hostvars[item]['swarm_info']['swarm_facts']['JoinTokens']['Manager'])"

# - name: Set default swarm manager
#   set_fact:
#     default_swarm_manager: "{{ groups['docker_swarm_managers'][0] }}"
#   run_once: true

- name: Fetch swarm status for managers
  command: docker info --format='{{ "{{json .Swarm}}" }}'
  register: docker_swarm_status
  when: "'docker_swarm_managers' in group_names"

# - debug: var=docker_swarm_status.stdout

- name: Parse swarm status
  set_fact:
    swarm_local_node_state: "{{ (docker_swarm_status.stdout | from_json).LocalNodeState }}"
    swarm_control_available: "{{ (docker_swarm_status.stdout | from_json).ControlAvailable }}"
  when: "'docker_swarm_managers' in group_names and docker_swarm_status.stdout | length > 0 and (docker_swarm_status.stdout | from_json).LocalNodeState is defined"

# Loop through managers to find an active one
- name: Find active swarm manager
  set_fact:
    active_swarm_manager: "{{ item }}"
    active_swarm_found: true
  loop: "{{ groups['docker_swarm_managers'] }}"
  when: >
    not active_swarm_found | default(false) and
    hostvars[item]['swarm_local_node_state'] is defined and
    hostvars[item]['swarm_local_node_state'] == 'active' and
    hostvars[item]['swarm_control_available'] | default(false)
  run_once: true

# Fetch tokens from the active manager if found
- name: Fetch tokens from active swarm manager
  community.docker.docker_swarm_info:
  register: swarm_info
  delegate_to: "{{ active_swarm_manager }}"
  when: >
    active_swarm_found | default(false) and
    hostvars[active_swarm_manager]['swarm_control_available'] | default(false)
  become: true

- name: Set join tokens from existing swarm
  set_fact:
    manager_join_token: "{{ swarm_info.swarm_facts.JoinTokens.Manager }}"
    worker_join_token: "{{ swarm_info.swarm_facts.JoinTokens.Worker }}"
  when: >
    active_swarm_found | default(false) and
    swarm_info is defined and 'swarm_facts' in swarm_info
  run_once: true

- debug:
    msg: "Manager token: {{ manager_join_token }}"
  when: manager_join_token is defined
  
- debug:
    msg: "Worker token: {{ worker_join_token }}"
  when: worker_join_token is defined
